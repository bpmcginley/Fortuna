{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the Fortuna main window. The app is front-end only, so no filesystem, shell, or network permissions are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
